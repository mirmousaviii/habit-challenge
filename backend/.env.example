# Server configuration
PORT=3000

# Repository type: memory or file
REPO_TYPE=file

# File storage path for habits (only used if REPO_TYPE=file)
DATA_FILE=data/habits.json

# Fake login credentials
FAKE_USERNAME=user1
FAKE_PASSWORD=pass1@habit
